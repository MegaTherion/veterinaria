{% extends 'AvanzuAdminThemeBundle:layout:base-layout.html.twig' %}

{% block page_title %}
    Nueva mascota
{% endblock %}

{% block page_content %}

    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            
        
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" class="btn btn-primary" />
    {{ form_end(form) }}
        </div>
    </div>
    
            <a class="btn btn-default" href="{{ path('mascota_index') }}">Regresar a la lista</a>
       
{% endblock %}
